name: ci
on:
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la lib || true
      - run: echo "myc-check optional until toolchain pin in umbrella"
